///scrDrawToBuffer()        //Stores all of the sprites in buffer to send to clients



scrDrawObjectToBuffer(objDragonDead);//Dead Dragon drawn behind everything

////////////Castle Background////////////
scrDrawObjectToBuffer(objCastleRight);
scrDrawObjectToBuffer(objGate);


/////////////Dead Ice Giant//////////////
scrDrawObjectToBuffer(objIceGiantDead)


////////////LargeEnemies///////////////
//Zeus
scrDrawObjectToBuffer(objZeusDead);
scrDrawObjectToBuffer(objZeus);
scrDrawObjectToBuffer(objZeusWet);
scrDrawObjectToBuffer(objFrozen);
scrDrawObjectToBuffer(objZeusCloud);
scrDrawObjectToBuffer(objDarkCloud);
//Dragon
scrDrawObjectToBuffer(objDragon);
//IceGiant
scrDrawObjectToBuffer(objIceGiant);
scrDrawObjectToBuffer(objIceDrop);
scrDrawObjectToBuffer(objHeadDrop);
//Troll
scrDrawObjectToBuffer(objTrollDead);
scrDrawObjectToBuffer(objTroll);
scrDrawObjectToBuffer(objTweenBoulder);

//SnowMan
scrDrawObjectToBuffer(objSnowManDead);
scrDrawObjectToBuffer(objSnowMan);

////////////DeadEnemies////////////////
scrDrawObjectToBuffer(objPeasantDead);
scrDrawObjectToBuffer(objFootSoldierDead);
scrDrawObjectToBuffer(objBlueMageDead);
scrDrawObjectToBuffer(objGreenMageDead);
scrDrawObjectToBuffer(objShieldHolderDead);
scrDrawObjectToBuffer(objCloudMinionDead);

/////////////FireEffect/////////////////////
scrDrawObjectToBuffer(objFireEffect);

//////////////MostEnemies///////////////////
scrDrawObjectToBuffer(objPeasant);
scrDrawObjectToBuffer(objFootSoldier);
scrDrawObjectToBuffer(objShieldHolder);
scrDrawObjectToBuffer(objBlueMage);
scrDrawObjectToBuffer(objGreenMage);
scrDrawObjectToBuffer(objCloudMinion);
scrDrawObjectToBuffer(objCloudMinionDark);
scrDrawObjectToBuffer(objWaterMinion);
scrDrawObjectToBuffer(objIceDropMinion);
scrDrawObjectToBuffer(objIceDropHead);

    
/////////////FrostEffect////////////////////////
scrDrawObjectToBuffer(objFrostEffect);
    
//////////////Enemy'sObjects////////////////////
scrDrawObjectToBuffer(objShield);
scrDrawObjectToBuffer(objShieldBurnUp);
scrDrawObjectToBuffer(objEnergyShield);
scrDrawObjectToBuffer(objHealAura);
scrDrawObjectToBuffer(objFireBall);
scrDrawObjectToBuffer(objSnowBall);
scrDrawObjectToBuffer(objSnowBallExplode);
scrDrawObjectToBuffer(objZeusRain);
scrDrawObjectToBuffer(objZeusLightning);

////////////////Coins//////////////////////////
scrDrawObjectToBuffer(objCoin);


///////////////Arrows/////////////////////////////
//scrDrawPlayerObjectToBuffer(objArrowParent);
scrDrawObjectToBuffer(objStuckArrow);
scrDrawObjectToBuffer(objBrokenArrow);

/////////////////LightningArcs////////////////////
scrDrawObjectToBuffer(objArcDraw);
scrDrawObjectToBuffer(objArcPoint);

/////////////////HPBars///////////////////////////
scrDrawObjectToBuffer(objHPBar);

///////////////CastleLeft////////////////////////
scrDrawObjectToBuffer(objCastleLeft);

////////////Defenders///////////////////
//scrDrawPlayerObjectToBuffer(objArcherParent);
//scrDrawPlayerObjectToBuffer(objBowParent);



/////////////CarnageReport/////////////////////
scrDrawObjectToBuffer(objBlack);
scrDrawObjectToBuffer(objCarnageReport);
scrDrawObjectToBuffer(objDamagePercentage);
scrDrawObjectToBuffer(objCarnageDisplayParent);


////////////ReadyIndicator/////////////
scrDrawObjectToBuffer(objReady);

////////////StringSprites//////////////
scrDrawObjectToBuffer(objNetStringParent);










